const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:54789', { waitUntil: 'networkidle0' }); await page.evaluate(() => { window.currentUser = { id: 'test', email: 'test@test.com' }; window.currentUserRole = 'EMPLOYEE'; window.currentUserProfile = { role: 'EMPLOYEE' }; window.setLoginMode('login'); const e = new Event('login'); window.dispatchEvent(e); document.querySelector('.topbar').style.display = 'flex'; }); await page.click('.user-profile'); await page.screenshot({ path: 'test_click.png' }); await browser.close(); })();
